# Documentation

This folder stores the assignment and methodology material used for the
Box-Jenkins forecasting and seasonal-adjustment project.

Current document:

- `box_jenkins_methodology_guidelines.pdf`: course guideline and methodological
  reference for the Box-Jenkins analysis.

The empirical implementation is in:

```text
../scripts/box_jenkins_forecasting_analysis.R
```

The reports and reproducible outputs are stored in `../report/`, `../figures/`,
and `../outputs/`.
